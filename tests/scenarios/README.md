# Cross-domain scenarios

No domain is production-ready merely because its isolated tests pass
(`00_MASTER_BUILD.md`, principle 11). Scenarios that exercise a documented
contract in `docs/integration/` live here, named after the contract they prove.

Empty until the first contract is agreed.
